const lesson = (id,track,title,topic,summary,sections,exercise,answer,code='',source='') => ({id,track,title,topic,description:summary,level:'Guided',contentStatus:'ready',sections,exercise,answer,code,reference:source});
export const lessons = [
lesson('java-collections','java','Collections and choosing a data structure','Foundations','Choose a collection from its access pattern and invariants.',[
 ['Start from the operations','ArrayList provides constant-time indexed reads and amortized constant-time append. Inserting near the front shifts elements. HashMap provides expected constant-time key lookup; its keys must maintain stable equals and hashCode behavior. TreeMap maintains sorted keys with logarithmic basic operations.'],
 ['Explain the trade-off','For a leaderboard that needs ordered ranges, a HashMap alone is not enough. For a simple id-to-record lookup, ordering from a TreeMap adds cost without solving an additional requirement. ArrayDeque is usually a better general-purpose stack or queue than a linked list.']
],'Choose a structure for counting words, then returning the top three. Explain time and space.','Count in a HashMap in O(n) expected time. Keep a size-3 min-heap of entries to select top counts in O(u log 3), where u is the number of unique words. Define deterministic tie ordering.',
 'Map<String,Integer> counts = new HashMap<>();\nfor (String word : words) {\n  counts.merge(word, 1, Integer::sum);\n}'),
lesson('java-equality','java','equals, hashCode, and immutable keys','Foundations','Understand why map lookups can fail after mutation.',[
 ['The contract','If two objects are equal, their hash codes must be equal. The reverse is not required: hash collisions are expected. HashMap uses the hash to locate candidates and equality to distinguish keys.'],
 ['Mutation changes lookup behavior','If a field used by hashCode changes while a key is in a map, future lookups can search a different bucket. Prefer immutable keys, such as a Java record with immutable components. Arrays inside records still require careful equality design.']
],'A Customer key is indexed by email. Its email changes after insertion. What can fail?','get and remove can fail because the object may now hash to a different bucket. Remove before mutation and reinsert, or use a stable immutable customer ID as the key.',
 'record CustomerKey(long id) {}\nMap<CustomerKey,String> names = new HashMap<>();\nnames.put(new CustomerKey(7), "Anand");\nSystem.out.println(names.get(new CustomerKey(7)));'),
lesson('java-concurrency','java','Concurrency: visibility and atomicity','Concurrency','Separate thread visibility from compound-operation safety.',[
 ['Different guarantees','volatile makes writes visible according to Java memory-model rules, but count++ is still a read-modify-write sequence. AtomicInteger supports atomic increments. synchronized protects a larger critical section and related invariants.'],
 ['Pick the invariant first','An atomic stock counter does not make a multi-step reservation transaction safe. If inventory decrement and reservation creation must succeed together, enforce that invariant at the database or within one properly protected boundary. Avoid holding locks across slow network calls.']
],'Why can volatile int count lose increments with two threads?','Both threads can read the same value, calculate the same successor, and overwrite one another. Use AtomicInteger.incrementAndGet for an independent counter, or a lock for an invariant spanning multiple fields.',
 'AtomicInteger count = new AtomicInteger();\ncount.incrementAndGet();\nint snapshot = count.get();'),
lesson('java-spring-transactions','java','Spring transactions and service boundaries','Spring Boot','Place atomic business operations at explicit transaction boundaries.',[
 ['Design the unit of work','A transfer should debit and credit within a single database transaction. A remote HTTP request is not automatically part of that transaction. Keep database transactions short and define what happens if downstream work fails.'],
 ['Proxy behavior matters','In Spring’s usual proxy mode, a call must pass through the proxy for transactional interception. A method calling another method on itself bypasses that proxy. Default rollback behavior covers unchecked exceptions; checked exceptions require an explicit rule when rollback is intended.'],
 ['Database plus messaging','Writing a row and publishing a Kafka message are two separate writes. An outbox record committed with the business data allows a relay to publish later. Consumers still need duplicate handling.']
],'An order row commits but publishing OrderCreated fails. How do you prevent losing the event?','Write order and outbox rows in one database transaction. A retrying relay publishes outbox events and records delivery progress. Use stable event IDs and idempotent consumers because retries can publish duplicates.',
 '@Transactional\npublic void createOrder(CreateOrder command) {\n  Order order = orders.save(toOrder(command));\n  outbox.save(OrderCreated.from(order));\n}', 'https://docs.spring.io/spring-framework/reference/data-access/transaction/declarative/annotations.html'),
lesson('java-api-design','java','REST APIs, retries, and idempotency','Spring Boot','Make retries safe without confusing HTTP success with business success.',[
 ['Design a stable contract','Define resource identifiers, validation rules, error shapes, and pagination. Use appropriate HTTP status codes, but also document domain error codes clients can handle. Return a created resource location where useful.'],
 ['Idempotency is a stored decision','For a retried create request, scope the idempotency key to the caller, store a payload fingerprint, and replay the original result. Reject a reused key with a different payload. A unique database constraint closes races between duplicate requests.']
],'Two identical checkout requests arrive simultaneously. What must be atomic?','Claiming the scoped idempotency key and creating the business result must have a race-safe protocol. A transaction plus a unique key can establish ownership; pending attempts must not create duplicate orders. Persist and replay the completed response.'),

lesson('ai-numpy-deep-dive','ai','NumPy Deep Dive','Week 1','Reason about shapes before writing vectorized operations.',[
 ['Broadcasting','Broadcasting compares dimensions from the right. Dimensions are compatible if they match or one is 1. A matrix with shape (batch, features) can subtract a feature mean of shape (features,), applying the same centering vector to every row.'],
 ['Performance and memory','Vectorization moves loops into optimized operations, but temporary arrays still consume memory. Broadcasting a pairwise distance calculation can create an enormous intermediate. Chunk the computation when memory is the limiting resource.']
],'Center each column of a matrix without a Python row loop. What shape should the mean have?','Use X - X.mean(axis=0, keepdims=True). The mean has shape (1, features), making the intended broadcast explicit. For an ML pipeline, compute that mean from training data only.',
 'import numpy as np\nX = np.array([[1., 10.], [3., 20.]])\nmean = X.mean(axis=0, keepdims=True)\ncentered = X - mean\n# [[-1., -5.], [1., 5.]]'),
lesson('ai-pandas-for-ml-i','ai','Pandas for ML I','Week 1','Treat joins as a change to your dataset’s unit of observation.',[
 ['Choose the grain','One row might mean a customer, an order, or an event. Joining orders to order lines increases row count. If the target remains at order level, aggregate line items before joining or explicitly accept the finer grain.'],
 ['Validate assumptions','A join on a nonunique lookup key can silently multiply examples. Validate many-to-one or one-to-one relationships and inspect unmatched rows. Sorting timestamps does not by itself prevent future information leaking into features.']
],'A customer table has duplicate customer IDs. What happens when joining it to orders?','An order can match multiple customer rows, multiplying training examples and distorting aggregates. Resolve the duplicate-key rule and use merge validation to catch violations.',
 'training = orders.merge(\n    customers, on="customer_id", how="left",\n    validate="many_to_one", indicator=True\n)\nassert len(training) == len(orders)'),
lesson('ai-model-evaluation-deep-dive','ai','Model Evaluation Deep Dive','Week 5','Pick evaluation metrics that reflect the decision being made.',[
 ['Splits come first','Random splits can leak information when the same user appears in both sets or when predicting future events. Use time-based or group-based splits where the deployment setting requires them. Fit preprocessing only on training data.'],
 ['Thresholds encode costs','Precision asks how many flagged cases are positive; recall asks how many positives are caught. PR curves are useful for rare positives. Choose a threshold on validation data using explicit false-positive and false-negative costs, then report final test results once.']
],'A fraud model has 99% accuracy on data with 1% fraud. Is it useful?','Not enough information: predicting no fraud also achieves 99%. Examine recall, precision, PR-AUC, calibration, and operational cost at a chosen threshold. Compare with a simple baseline under the same split.'),
lesson('ai-self-attention-scaled-dot-product','ai','Self-Attention & Scaled Dot-Product','Week 9','Trace query, key, and value shapes through attention.',[
 ['What attention computes','Project token representations into Q, K, and V. QKᵀ measures pairwise compatibility. Divide scores by the square root of key dimension, mask disallowed positions, normalize each query row with softmax, then combine values.'],
 ['Cost and constraints','Naive full attention has a quadratic score matrix in sequence length. A causal mask prevents future-token access. Efficient attention implementations can avoid materializing the full score matrix, but do not make all pairwise computation disappear.']
],'With Q and K shaped (8,64), what is the score matrix shape? Why mask before softmax?','The score matrix is (8,8). Set disallowed scores to negative infinity before normalization so they receive zero probability. Masking after softmax without renormalization changes the row sum.',
 'scores = (Q @ K.T) / np.sqrt(Q.shape[-1])\n# Apply a task-appropriate mask here.\nscores = scores - scores.max(axis=-1, keepdims=True)\nweights = np.exp(scores)\nweights /= weights.sum(axis=-1, keepdims=True)\noutput = weights @ V'),
lesson('ai-rag-architecture-tradeoffs','ai','RAG Architecture & Tradeoffs','Week 11','Separate retrieval failure from generation failure.',[
 ['Build two pipelines','Ingestion extracts documents, chunks text, stores metadata, and indexes embeddings. At question time retrieve relevant chunks, optionally rerank, then generate an answer grounded in those chunks. Version documents and embeddings so updates are traceable.'],
 ['Measure each stage','If the right evidence never enters the context, better generation prompts cannot fix retrieval recall. Evaluate retrieval recall at k separately from answer correctness and citation support. A larger k can improve recall while increasing latency, token cost, and distracting context.'],
 ['Access control belongs in retrieval','Filter by the caller’s permissions before exposing content to the model. Treat retrieved text as untrusted data: it must not be allowed to redefine tool permissions or system instructions.']
],'Users receive fluent but incorrect answers. What do you inspect first?','Inspect retrieved passages against labeled supporting evidence. If evidence is absent, improve chunking, query formulation, filtering or retrieval. If evidence is present, inspect grounding instructions, conflicting passages, citation fidelity and abstention behavior.'),
lesson('ai-agents-intro','ai','Agents Intro','Week 15','Make tool use bounded, inspectable, and recoverable.',[
 ['Agent versus workflow','A workflow follows a predetermined sequence. An agent chooses among actions based on intermediate results. Dynamic choice is useful for uncertain tasks, but it adds failure modes and evaluation complexity. Start with a deterministic workflow when the steps are known.'],
 ['Tools are contracts','Validate tool arguments against schemas, apply authorization outside the model, and distinguish retries from repeated business actions. Set execution and cost budgets. Require confirmation for actions that need user approval. Log tool results and errors without exposing secrets.']
],'An agent retries a payment tool after a timeout. How do you avoid a double charge?','Use a stable idempotency key and reconcile the first attempt’s status before issuing a new operation. A timeout is an unknown outcome, not proof the payment failed. Enforce this in the tool backend.'),

lesson('react-state','react','useState and immutable updates','State','Model the smallest state necessary to render the interface.',[
 ['State is a render snapshot','An event handler sees values from the render that created it. When the next value depends on the previous value, use a functional updater. Replace arrays and objects rather than mutating existing state.'],
 ['Avoid duplicated state','If a filtered list can be calculated from items and query, derive it during rendering. Storing a separate filtered list creates another value that must stay synchronized.']
],'Two consecutive setCount(count + 1) calls run in one handler. How do you request two increments safely?','Use setCount(c => c + 1) twice. Functional updaters are applied to the queued state in order, instead of both reading the same rendered count.',
 'setItems(items => items.map(item =>\n  item.id === targetId ? { ...item, done: true } : item\n));'),
lesson('react-reducer','react','useReducer and predictable transitions','State','Keep related state transitions together and test them as pure functions.',[
 ['Actions describe what happened','A reducer receives state and an action and returns the next state. Actions such as itemAdded or checkoutFailed express intent. Keep network requests, time reads, and storage writes outside the reducer.'],
 ['When a reducer helps','useState is enough for independent small values. useReducer helps when several fields must change together or transitions become hard to reason about. It does not automatically provide global state or persistent storage.']
],'Write a reducer action that removes a cart item and keeps the rest unchanged.','Return a new state with items filtered by ID. Derive the total from current items instead of storing a second total unless there is a clear reason.',
 'function reducer(state, action) {\n  switch (action.type) {\n    case "removed":\n      return { ...state, items: state.items.filter(x => x.id !== action.id) };\n    default:\n      return state;\n  }\n}'),
lesson('react-effects','react','Effects, dependencies, and cleanup','Hooks','Use effects to synchronize with external systems.',[
 ['Choose the right place','Calculate derived values during rendering. Handle a purchase caused by a button click in its event handler. Use an effect for subscriptions or other synchronization that must track rendered state.'],
 ['Cleanup mirrors setup','Include reactive dependencies and clean up subscriptions when they change or the component unmounts. Development Strict Mode performs an extra setup/cleanup cycle to expose missing cleanup.']
],'An interval continues after a component disappears. What is missing?','Return a cleanup function that clears the interval. For state updates based on prior state, use the functional updater.',
 'useEffect(() => {\n  const timer = setInterval(() => setCount(c => c + 1), 1000);\n  return () => clearInterval(timer);\n}, []);', 'https://react.dev/reference/react/useEffect'),
lesson('react-performance','react','Rendering performance and stable keys','Performance','Measure before adding memoization.',[
 ['Identity matters','Keys identify siblings across renders. Stable data IDs preserve the association between component state and records. Array indices can attach state to the wrong record when items are reordered or removed.'],
 ['Optimize the actual bottleneck','A slow list might need virtualization, smaller data payloads, or fewer expensive calculations. Memoization has comparison and memory costs. Profile representative interactions before applying it broadly.']
],'An editable list is sorted and text inputs appear to swap values. What would you inspect?','Inspect the keys. If index keys are used, component identity follows position instead of record identity. Use a stable unique record ID and confirm each controlled value updates the intended record.'),

lesson('angular-components','angular','Components and template boundaries','Foundations','Keep rendering responsibilities small and explicit.',[
 ['Component contracts','A component combines view logic and a template. Inputs carry data in; outputs report events out. A presentation component should not silently own application-wide mutations that its parent cannot observe.'],
 ['Dependency injection','Services can encapsulate API access or shared behavior. Provider scope controls instance lifetime. A component-scoped provider creates a different sharing boundary from a root-provided service.']
],'Should a reusable product card fetch the entire product catalog?','Usually the parent or a data service owns loading and provides the card’s data. The card emits an add-to-cart event. This keeps loading, empty and error states consistent and the card easier to test.'),
lesson('angular-signals','angular','Signals and computed state','State','Represent synchronous reactive values and their derivations.',[
 ['Writable versus computed','A writable signal stores a value; a computed signal derives a value from signals it reads. Computed values are lazy and memoized. Update source state rather than manually synchronizing a duplicate derived value.'],
 ['Choose the tool by behavior','Signals suit current synchronous state. Observable streams are useful for time-dependent events and asynchronous composition. They can work together rather than replacing each other everywhere.']
],'Store quantity and price, then derive subtotal without an effect.','Use writable signals for quantity and price and a computed signal that multiplies them.',
 'const quantity = signal(2);\nconst price = signal(15);\nconst subtotal = computed(() => quantity() * price());\nquantity.update(q => q + 1);', 'https://angular.dev/guide/signals'),
lesson('angular-rxjs','angular','RxJS request cancellation and ordering','Async','Pick an operator based on whether newer work supersedes older work.',[
 ['Different flattening policies','switchMap replaces the previous inner subscription. concatMap queues inner work. mergeMap allows concurrent work. exhaustMap ignores new triggers while an inner subscription is active. These are different product behaviors, not interchangeable syntax.'],
 ['Cancellation has limits','Unsubscribing from an HTTP observable can cancel the client request, but it cannot guarantee a server-side mutation was rolled back. Use switchMap for replaceable searches, not as a guarantee against duplicate payment writes.']
],'A search box shows results for an old query after a new query. Which behavior do you need?','Debounce query changes, ignore duplicates, and switch to the newest request. Handle errors inside the inner stream if later queries should continue working. Validate cancellation behavior with the actual HTTP client.'),
lesson('angular-forms','angular','Reactive forms and validation','Forms','Separate client feedback from server enforcement.',[
 ['Model the form','A reactive form exposes explicit controls and validation state. Show errors after meaningful interaction or submission. Disable or guard duplicate submissions, and preserve entered values when the server rejects a request.'],
 ['Server validation remains necessary','A user can bypass browser validation. The server must enforce required fields, authorization, length and format constraints, and business invariants. Async availability checks can go stale before submission.']
],'A username passes an availability check, but submission reports it is taken. Is this a bug?','Not necessarily: another user may have claimed it between check and submit. Enforce uniqueness with a database constraint and display a field-specific conflict response.'),

lesson('system-url-shortening-service-like-tinyurl','system','URL Shortening service like TinyURL','Architecture','Design a read-heavy redirect service and explain collision handling.',[
 ['Clarify the contract','Support create-short-link and resolve-code operations. Decide whether links expire, whether owners can edit them, and whether analytics may lag. Redirect latency and availability often matter more than immediate analytics.'],
 ['Baseline architecture','A load balancer routes to stateless API replicas. A durable key-value mapping stores code to destination with a unique code constraint. A cache serves hot redirects. Send click events asynchronously to analytics so analytics failures do not block redirects.'],
 ['Trade-offs','Random codes reduce centralized coordination but require collision detection and retry. Sequence-derived codes are simple but expose ordering and require an allocation strategy. Permanent redirects can be cached aggressively by clients, complicating edits and revocation; temporary redirects offer more control.']
],'At 10 million redirects per day and a 10× peak multiplier, estimate peak requests per second.','Average is about 116 requests/s; peak is about 1,158 requests/s. These are workload estimates, not capacity guarantees. Benchmark the cache, database fallback and network at realistic percentiles.'),
lesson('system-api-rate-limiter','system','API Rate Limiter','Architecture','Define burst behavior, consistency, and failure policy.',[
 ['Specify what is limited','Choose a caller key, limit scope, refill or window duration, and response semantics. Per-user and per-IP limits protect against different behaviors. Shared NAT users can make IP-only limits unfair.'],
 ['Token bucket versus fixed window','A token bucket refills over time and permits bounded bursts. Fixed windows are simpler but allow large bursts near boundaries. Sliding logs track exact events with higher storage cost; sliding counters approximate with less memory.'],
 ['Distributed enforcement','Multiple API replicas need a shared atomic decision or partitioned ownership. A Redis script can update tokens and timestamp together. Define fail-open versus fail-closed behavior based on endpoint risk and availability needs.']
],'A bucket has capacity 20 and refills at 5 tokens/s. It is empty. How many requests can pass after 3 idle seconds?','15 requests can pass immediately, assuming one token per request. After 10 idle seconds it still caps at 20. Time sources and atomic updates must be consistent in a distributed implementation.'),
lesson('system-facebook-s-newsfeed','system','Facebook’s Newsfeed','Architecture','Compare write-time and read-time feed assembly.',[
 ['Fan-out on write','When an author posts, append the post reference to followers’ feed indexes. Reads become cheap, but high-follower accounts cause large write amplification. Deleting or changing visibility requires careful propagation.'],
 ['Fan-out on read','Read recent posts from followed authors at request time, then merge and rank. Posting is cheap, but readers following many authors need more work. A hybrid can fan out ordinary accounts and merge high-follower accounts at read time.'],
 ['Pagination and consistency','Use stable cursors rather than offsets for a changing feed. Recheck access permissions when rendering. Eventual propagation is often acceptable for delivery, while blocking revoked content may require a stricter authorization path.']
],'An author has 50 million followers. Would you synchronously write every follower’s feed before acknowledging a post?','No. Persist the post first and use asynchronous work or read-time merging. Bound the fan-out workload and consider a hybrid policy based on fan-out cost, active followers and read frequency.')
];
